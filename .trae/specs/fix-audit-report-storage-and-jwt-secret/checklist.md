# Checklist

- [x] 已验证 `_audit_reports` 全局变量确实被审计报告接口使用，或已记录不存在原因。
- [x] 审计报告不再存储在模块级全局变量中。
- [x] 审计报告生成后保存到数据库。
- [x] 审计报告可按导入任务 ID 从数据库查询。
- [x] 不同导入任务的审计报告互不串扰。
- [x] 本地 SQLite 启动时具备审计报告表结构兼容能力。
- [x] 已验证 JWT 默认开发密钥回退确实存在，或已记录不存在原因。
- [x] `security.py` 不再包含 `dev-secret-key-do-not-use-in-production` 硬编码回退。
- [x] 未配置 `secret_key` 时不会签发 JWT。
- [x] 未配置 `secret_key` 时不会使用默认密钥解析 JWT。
- [x] 配置 `secret_key` 后 JWT 签发和解析正常。
- [x] 审计报告相关后端测试通过。
- [x] 认证安全相关后端测试通过。
- [x] 前端类型检查已执行且未受本次后端安全修复影响。
